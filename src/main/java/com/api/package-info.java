/**
 * The package that contains all the api calls that are done via tidecli, and processes the returned information
 * <p>
 *  This package contains the two classes needed to do all the api calls such as logging into tide, getting the courses with tide tasks,
 *  downloading the tasks, submitting the answers, returning the points/remarks given by tim and also handling the json formatting that the client returns.
 * </p>
 * @since 1.0
 * @author tero
 * @version 1.0
 *
 */
package com.api;